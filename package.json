{
  "name": "ticketing-demo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "npm --workspace apps/api test",
    "arch": "npx dependency-cruiser --config .dependency-cruiser.js apps"
  }
}
